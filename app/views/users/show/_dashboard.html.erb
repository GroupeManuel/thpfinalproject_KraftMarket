<div class="row">

	<!-- Statistic on current items online -->
	<div class="col-md-6">
	<%= link_to user_sales_path(current_user.id) do %>
			<div class="dashboard-stat color-1">
				<div class="dashboard-stat-content"><h4><% current_user.items.where(status:'published').count %></h4> <span>Annonces en ligne</span></div>
				<div class="dashboard-stat-icon"><i class="fa fa-tags"></i></div>
			</div>
	<% end %>
		</div>
	
	<!-- Item -->
	<div class="col-md-6">
		<div class="dashboard-stat color-3">
			<div class="dashboard-stat-content"><h4></h4> <span>L'évaluation des vendeurs arrive bientôt sur Kraft Market</span></div>
			<div class="dashboard-stat-icon"><i class="fa fa-star-half-alt"></i></div>
		</div>
	</div>

	<!-- Statistic on purchases -->

	<div class="col-md-6">
	<%= link_to user_orders_path(current_user.id) do %>
		<div class="dashboard-stat color-4">
			<div class="dashboard-stat-content"><h4><% current_user.orders.count %></h4> <span>Objets recyclés</span></div>
			<div class="dashboard-stat-icon"><i class="fa fa-tags"></i></div>
		</div>
	<% end %>
	</div>

	<!-- Item -->
	<div class="col-md-6">
		<div class="dashboard-stat color-2">
			<div class="dashboard-stat-content"><h4></h4> <span>L'ajout de vos vendeurs favoris arrive bientôt sur Kraft Market</span></div>
			<div class="dashboard-stat-icon"><i class="fa fa-bookmark"></i></div>
		</div>
	</div>
</div>
